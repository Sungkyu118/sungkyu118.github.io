---
layout: post
title: "Vue 입문 125: defineAsyncComponent로 느린 컴포넌트 분리하기"
date: 2026-07-05 13:05:00 +0900
category: Vue
permalink: /vue/intro-125-async-component-loading-error
description: "Vue의 defineAsyncComponent를 사용해 무거운 컴포넌트를 지연 로딩하고 loading, error UI를 함께 다루는 방법을 설명합니다."
image:
  path: "/assets/img/og/vue-series-cover.svg"
  alt: "Vue 입문 시리즈 대표 이미지"
tags: ["Vue", "Async Component", "성능"]
---

# Vue 입문 125: defineAsyncComponent로 느린 컴포넌트 분리하기

> 라우트가 아니더라도 차트, 에디터, 지도처럼 무거운 컴포넌트는 필요한 순간에만 불러오는 편이 좋을 수 있습니다.
>
> 이전 글: [Vue 입문 124: 라우트 단위 코드 스플리팅 점검하기](/vue/intro-124-route-code-splitting-checklist)
> 다음 글: [Vue 입문 126: props 안정성으로 불필요한 렌더링 줄이기](/vue/intro-126-props-stability-render-update)
> 함께 보면 좋은 글:
> - [Vue 입문 124: 라우트 단위 코드 스플리팅 점검하기](/vue/intro-124-route-code-splitting-checklist)
> - [Vue 입문 126: props 안정성으로 불필요한 렌더링 줄이기](/vue/intro-126-props-stability-render-update)
> - [Vue 입문 121: 성능 최적화는 측정부터 시작하기](/vue/intro-121-performance-measure-before-fix)

라우트가 아니더라도 차트, 에디터, 지도처럼 무거운 컴포넌트는 필요한 순간에만 불러오는 편이 좋을 수 있습니다.

대시보드에서 차트 탭을 열 때만 차트 컴포넌트를 가져오고, 로딩 중과 실패 시 화면을 따로 보여주는 예제를 작성하겠습니다.

이 글에서는 단순히 “이렇게 쓰면 됩니다”에서 멈추지 않고, 왜 이런 선택을 하는지, 언제 도움이 되는지, 그리고 언제 오히려 문제를 만들 수 있는지를 함께 보겠습니다. 입문 단계에서는 문법을 많이 외우는 것보다 상황을 나누어 생각하는 힘이 훨씬 오래 갑니다. Vue는 문법 자체가 비교적 부드러운 편이지만, 프로젝트가 커지면 결국 상태, 화면, 서버, 빌드, 접근성, 운영이라는 여러 기준이 동시에 움직이기 때문입니다.

## 이번 글에서 먼저 잡을 관점

이번 성능 구간에서는 “빠르게 보이는 화면”과 “사용자 입력에 부드럽게 반응하는 화면”을 나누어 생각하겠습니다. 성능은 감으로 고치는 영역이 아니라 측정, 원인 분리, 작은 실험, 다시 측정의 순서로 접근해야 합니다. Vue는 대부분의 일반적인 화면에서 충분히 빠르지만, 목록이 커지거나 의존성이 늘거나 빌드 결과가 커지는 순간부터는 설계 습관이 차이를 만들기 시작합니다.

지금 단계에서는 완벽한 구조를 처음부터 외우려고 하지 않으셔도 괜찮습니다. 대신 “이 코드는 어떤 문제를 줄이려고 있는가”, “이 선택 때문에 새로 생기는 비용은 무엇인가”, “문제가 생겼을 때 어디부터 확인해야 하는가”를 말로 설명하는 연습을 해보시면 좋습니다. 이렇게 생각하면 예제 코드를 그대로 복사하는 수준을 넘어서, 내 프로젝트 상황에 맞게 조정할 수 있는 힘이 생깁니다.

본문 중간에 연결해서 보면 좋은 흐름도 하나 짚고 가겠습니다. [Vue 입문 124: 라우트 단위 코드 스플리팅 점검하기](/vue/intro-124-route-code-splitting-checklist) 글을 먼저 다시 보면 지금 글이 갑자기 튀어나온 주제가 아니라는 점이 보입니다. 이전 글에서 다룬 기준이 지금 글에서 더 실무적인 판단으로 확장되기 때문입니다. 반대로 지금 글이 어느 정도 이해된다면, 다음 글로 넘어가면서 같은 기준이 다른 문제에서 어떻게 적용되는지 확인해보시면 좋습니다.

## 작은 실습으로 확인해보기

아래 예제는 실제 프로젝트에서 바로 응용할 수 있는 작은 형태로 구성했습니다. 예제를 볼 때는 코드 줄 수보다 책임의 위치를 먼저 봐주세요. 어떤 값이 입력이고, 어떤 함수가 상태를 바꾸고, 어떤 결과가 화면이나 빌드 결과에 영향을 주는지 따라가면 훨씬 이해가 쉽습니다.

{% raw %}
~~~vue
<script setup>
import { defineAsyncComponent } from 'vue'
import ChartSkeleton from './ChartSkeleton.vue'
import ChartLoadError from './ChartLoadError.vue'

const SalesChart = defineAsyncComponent({
  loader: () => import('./SalesChart.vue'),
  loadingComponent: ChartSkeleton,
  errorComponent: ChartLoadError,
  delay: 200,
  timeout: 10000
})
</script>

<template>
  <section>
    <h2>월별 매출</h2>
    <SalesChart />
  </section>
</template>
~~~
{% endraw %}

## 코드가 움직이는 순서

첫 번째로 확인할 것은 “어떤 시점에 실행되는 코드인가”입니다. Vue 앱에서는 같은 코드처럼 보여도 컴포넌트가 렌더링될 때 실행되는 코드, 사용자가 클릭했을 때 실행되는 코드, 빌드 시점에 결정되는 코드, 운영 브라우저에서 실행되는 코드가 서로 다릅니다. 이 시점을 구분하지 못하면 문제를 고칠 때 엉뚱한 곳을 보게 됩니다.

두 번째로 확인할 것은 “이 코드가 사용자 경험에 어떤 영향을 주는가”입니다. 성능 최적화라면 사용자가 더 빨리 화면을 보는지, 접근성이라면 키보드와 보조 기술 사용자가 막히지 않는지, 타입이라면 잘못된 데이터 모양을 더 빨리 발견하는지, 배포라면 로컬과 운영의 차이를 줄이는지 확인해야 합니다.

세 번째로 확인할 것은 “실패했을 때 어떤 모습인가”입니다. 좋은 코드는 성공할 때만 예쁜 코드가 아니라 실패했을 때도 원인을 찾을 수 있는 코드입니다. 예제 코드를 직접 따라 작성한 뒤에는 일부러 잘못된 값, 느린 네트워크, 빈 데이터, 잘못된 경로를 넣어보세요. 이 연습을 해보면 문서에서 읽은 설명이 훨씬 현실적인 감각으로 바뀝니다.

## 자주 만나는 에러와 유의사항

- **확인 포인트**: loadingComponent 없이 지연 로딩만 적용하면 사용자는 클릭이 먹히지 않는다고 느낄 수 있습니다. 입문 예제에서는 작게 보일 수 있지만, 실제 프로젝트에서는 원인을 찾기 어려운 버그나 사용자 경험 문제로 이어질 수 있습니다. 코드를 작성한 뒤에는 이 문제가 어떤 상황에서 나타나는지 직접 한 번 재현해보는 습관을 들이면 좋습니다.
- **확인 포인트**: 네트워크 실패를 고려하지 않으면 chunk 로딩 실패가 빈 화면으로 보일 수 있습니다. 입문 예제에서는 작게 보일 수 있지만, 실제 프로젝트에서는 원인을 찾기 어려운 버그나 사용자 경험 문제로 이어질 수 있습니다. 코드를 작성한 뒤에는 이 문제가 어떤 상황에서 나타나는지 직접 한 번 재현해보는 습관을 들이면 좋습니다.
- **확인 포인트**: 너무 작은 컴포넌트까지 비동기로 나누면 코드 구조가 복잡해지고 이득은 거의 없을 수 있습니다. 입문 예제에서는 작게 보일 수 있지만, 실제 프로젝트에서는 원인을 찾기 어려운 버그나 사용자 경험 문제로 이어질 수 있습니다. 코드를 작성한 뒤에는 이 문제가 어떤 상황에서 나타나는지 직접 한 번 재현해보는 습관을 들이면 좋습니다.

문제가 생겼을 때는 한 번에 모든 코드를 의심하지 말고, 입력값, 실행 시점, 상태 변경, 렌더링 결과, 빌드 결과를 차례로 확인해보세요. 특히 이번 구간의 주제들은 “개발 서버에서는 괜찮았는데 운영에서 깨지는 문제”와 자주 연결됩니다. 그래서 로컬 화면만 보는 것보다 빌드 결과, 브라우저 Network 탭, 콘솔 경고, 실제 HTML 구조까지 함께 보는 습관이 중요합니다.

## 실무에서 생각할 점

비동기 컴포넌트는 초기 로딩을 줄여주지만 사용자 행동 이후 기다림을 만들 수 있습니다. 그래서 loading UI, retry, 실패 안내까지 함께 설계해야 실무에서 안전합니다.

실무에서는 정답 패턴을 한 번에 고르는 것보다 현재 팀과 프로젝트의 복잡도에 맞는 선택을 하는 것이 중요합니다. 작은 화면에서는 단순한 코드가 가장 좋은 코드일 수 있고, 여러 화면에서 같은 문제가 반복되기 시작하면 그때 분리하는 편이 더 안전할 수 있습니다. 반대로 인증, 결제, 관리자 권한, 접근성, 배포 자동화처럼 실수의 영향이 큰 영역은 초반부터 기준을 조금 더 엄격하게 잡는 것이 좋습니다.

또 하나 중요한 점은 “최적화”라는 단어를 너무 좁게 보지 않는 것입니다. 빠르게 만드는 것도 최적화지만, 실수를 줄이는 타입 설계, 사용자를 막지 않는 접근성, 배포 후 문제를 찾을 수 있는 로그 기준도 모두 넓은 의미의 최적화입니다. 결국 좋은 Vue 앱은 문법을 많이 쓴 앱이 아니라 사용자가 안정적으로 목적을 달성할 수 있는 앱입니다.

## 디버깅하거나 검증할 때 확인할 순서

1. 지금 문제를 사용자의 행동으로 다시 설명합니다.
2. 개발 서버와 production build 중 어디에서 재현되는지 확인합니다.
3. 브라우저 콘솔과 Network 탭에서 오류, 404, 느린 요청을 확인합니다.
4. 컴포넌트 상태, props, emit, store, router 중 어느 경계에서 값이 달라지는지 확인합니다.
5. 접근성 주제라면 마우스를 쓰지 않고 Tab, Enter, Space, ESC만으로 흐름을 확인합니다.
6. 배포 주제라면 새로고침, 직접 URL 접근, 정적 파일 경로, 환경변수 값을 확인합니다.
7. 수정 후에는 같은 문제가 다시 생기지 않도록 테스트, 체크리스트, 문서 중 하나로 남깁니다.

이 순서를 습관처럼 적용하면 문제가 생겼을 때 훨씬 덜 흔들립니다. 입문 단계에서 가장 큰 차이는 “모든 API를 외웠는가”가 아니라 “어디부터 확인할지 알고 있는가”에서 생깁니다.

## 공식 문서와 같이 확인하면 좋은 부분

- [Vue Async Components](https://vuejs.org/guide/components/async.html)
- [Vue Performance Guide](https://vuejs.org/guide/best-practices/performance.html)

공식 문서는 문법의 기준점입니다. 블로그 글로 흐름을 잡고, 공식 문서로 정확한 옵션과 예외를 확인하면 오래된 예제나 다른 스타일의 코드를 만났을 때도 덜 흔들립니다. 특히 성능, 접근성, TypeScript, 배포 관련 코드는 도구 버전과 브라우저 환경의 영향을 받기 때문에 현재 공식 문서를 기준으로 다시 확인하는 습관을 추천드립니다.

## 오늘 내용 정리

이번 글에서는 'Vue 입문 125: defineAsyncComponent로 느린 컴포넌트 분리하기' 주제를 중심으로 Vue 프로젝트에서 자주 만나는 실무적인 판단 지점을 살펴봤습니다. 핵심은 코드를 더 복잡하게 만드는 것이 아니라, 문제를 만났을 때 확인할 순서와 선택 기준을 분명히 하는 것입니다. 예제 코드를 그대로 따라 작성한 뒤에는 이름을 바꿔보거나, 실패 상황을 일부러 만들어보거나, 조건을 하나 추가해보면서 흐름을 직접 확인해보시면 좋습니다.

다음 글로 넘어가기 전에 오늘 예제에서 “이 코드는 개발 시점의 문제를 해결하는가, 런타임 문제를 해결하는가, 빌드와 배포 문제를 해결하는가, 아니면 사용자 경험 문제를 해결하는가”를 한 번 말로 정리해보세요. 이 질문에 답하는 연습이 쌓이면 Vue 프로젝트가 커졌을 때도 무작정 패턴을 추가하거나 한 파일에 모두 몰아넣는 실수를 줄일 수 있습니다.

## 이어서 읽어보시면 좋습니다

### [Vue 입문 124: 라우트 단위 코드 스플리팅 점검하기](/vue/intro-124-route-code-splitting-checklist)

이전 글에서 다룬 흐름을 다시 확인하는 용도로 좋습니다. 지금 글은 앞에서 만든 개념 위에 한 단계 더 실무적인 기준을 얹는 구조라서, 예제의 전제가 빠르게 느껴진다면 이 글로 돌아가 기본 흐름을 다시 잡아보시면 좋습니다. 특히 Vue 시리즈는 컴포넌트, 라우터, 상태, API, 테스트, 성능, 배포가 서로 끊어져 있지 않고 이어지기 때문에 앞 글을 다시 읽는 것이 단순 복습이 아니라 현재 글의 판단 기준을 더 선명하게 만들어줍니다.

### [Vue 입문 126: props 안정성으로 불필요한 렌더링 줄이기](/vue/intro-126-props-stability-render-update)

다음 흐름으로 이어서 읽기 좋은 글입니다. 지금 글에서 개념의 이유와 기본 구조를 잡았다면, 이 글에서는 그 기준이 다른 상황에서 어떻게 확장되는지 확인할 수 있습니다. 한 번에 모든 내용을 외우려고 하기보다, 현재 글에서 만든 질문을 가지고 다음 글을 읽어보세요. 그러면 새 문법을 추가로 배우는 느낌보다 같은 문제를 조금 더 넓은 시야에서 바라보는 느낌으로 이해가 이어집니다.

### [Vue 입문 121: 성능 최적화는 측정부터 시작하기](/vue/intro-121-performance-measure-before-fix)

이전 글에서 다룬 흐름을 다시 확인하는 용도로 좋습니다. 지금 글은 앞에서 만든 개념 위에 한 단계 더 실무적인 기준을 얹는 구조라서, 예제의 전제가 빠르게 느껴진다면 이 글로 돌아가 기본 흐름을 다시 잡아보시면 좋습니다. 특히 Vue 시리즈는 컴포넌트, 라우터, 상태, API, 테스트, 성능, 배포가 서로 끊어져 있지 않고 이어지기 때문에 앞 글을 다시 읽는 것이 단순 복습이 아니라 현재 글의 판단 기준을 더 선명하게 만들어줍니다.
